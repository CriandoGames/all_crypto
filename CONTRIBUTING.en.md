# Contributing

Use Dart compatible with the minimum SDK, run `dart format .`, `dart analyze`, `dart test`, and `dart pub publish --dry-run`. Algorithm or serialization changes require reproducible vectors, backward-compatibility tests, and a security rationale. Never include keys, tokens, real payloads, or personal data in issues or fixtures.

